import { Text, View } from "react-native";

export default function NotFoundPage() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-primary-10 text-heading-small font-bold">
        Not Found Page
      </Text>
    </View>
  );
}
